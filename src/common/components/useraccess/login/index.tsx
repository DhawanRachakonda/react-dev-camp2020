import React from 'react';

import AppTemplate from '../../Template';
import LoginComponent from './LoginComponent';

export default () => (
  <AppTemplate>
    <LoginComponent />
  </AppTemplate>
);
